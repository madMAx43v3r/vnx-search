package vnx.search;

module PageProcessor {
	
	vnx.TopicPtr input = "frontend.text_responses";			// TextResponse
	
	string page_index_server = "PageIndex";
	
	string page_content_server = "PageContent";
	
	
	void handle(TextResponse sample);
	
}
