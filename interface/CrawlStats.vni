package vnx.search;

class CrawlStats {
	
	long num_fetched;
	
	long num_errors;
	
	long num_reload;
	
	long num_domains;
	
	long num_queued;
	
	long num_waiting;
	
	vector<crawl_domain_stats_t> most_fetched;
	
	vector<crawl_domain_stats_t> most_queued;
	
}
