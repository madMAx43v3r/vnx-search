package vnx.search;

struct query_options_t {
	
	int limit = 10;					// number of results
	
	uint offset;					// result offset
	
	int context = 20;				// number of words
	
	int max_group_size = 1;			// when grouping results
	
	uint max_results = 1000;		// max number of results for computation
	
	float score_power = 2;			// non-linear score factor
	
	score_type_e score_type = MAX_SCORE;
	
	vector<search_flags_e> flags;
	
}
