package vnx.search;

class PageInfo {
	
	uint id;						// page id (0 = not loaded yet)
	
	ulong version;					// version regarding title, etc
	
	ulong link_version;				// version regarding links
	
	ulong word_version;				// word context version
	
	string scheme;
	
	string title;
	
	vector<uint> words;
	
	vector<uint> links;
	
	vector<uint> reverse_links;
	
}
